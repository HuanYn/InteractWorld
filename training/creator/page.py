"""Standalone Creator UI. Dynamic content is assigned through textContent/value."""

HTML = '''<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="color-scheme" content="dark"><title>InterActWorld Creator · 用语言编排视频</title>
<link rel="stylesheet" href="/style.css"></head><body><main>
<header><div class="brand"><span class="brand-mark" aria-hidden="true">I/W</span><span>INTERACTWORLD <b>CREATOR</b></span><a href="/" aria-label="Creator 首页">创作工作台</a></div>
<div class="eyebrow">LANGUAGE → ACTION PLAN → VIDEO</div><h1>说出动作，<em>看见下一幕。</em></h1>
<p class="lede">从一张固定首图出发，把自然语言整理为动作计划，再用自训模型生成视频。</p>
<div class="badges"><span>异步生成 · 非实时</span><span>修改从同一首图重新生成</span><span>先编排，再确认生成</span></div>
<div class="capabilities"><span id="planner-kind">正在读取编排器…</span><span id="observer-kind">正在读取审核方式…</span></div></header>
<div id="notice" class="notice" role="status" aria-live="polite" hidden></div>
<div class="workspace-heading"><div><h2>创作会话</h2><p class="muted small">同一会话固定场景和随机种子；新的描述会创建新版本。</p></div><div class="session-controls"><label class="sr-only" for="session">恢复创作会话</label><select id="session"><option value="">新会话</option></select><button id="new-session" type="button">＋ 新会话</button></div></div>
<div class="compose-grid">
<section class="scene-panel" aria-labelledby="scene-heading"><div class="section-title"><span class="step">01</span><h2 id="scene-heading">固定起点</h2></div>
<label for="scene">场景</label><select id="scene" disabled></select>
<div class="initial-wrap"><img id="initial" alt="模型实际使用的固定首图"><span class="image-caption">INITIAL FRAME / 固定首图</span></div>
<label for="prompt">静态场景提示词 · 冻结只读</label><textarea id="prompt" rows="5" readonly></textarea>
<p class="muted small">动作描述不会改写这段提示词。每个版本都从这张首图重新生成。</p>
<label for="seed">随机种子</label><input id="seed" type="number" min="0" max="4294967295" step="1" value="42" disabled>
<details><summary>查看固定输入来源</summary><p id="source" class="small"></p></details></section>
<section class="composer" aria-labelledby="compose-heading"><div class="section-title"><span class="step">02</span><h2 id="compose-heading">描述你想做的动作</h2></div>
<div id="planner-mode-control" hidden><label for="planner-mode">本次编排方式（不会改变已有计划）</label><select id="planner-mode"><option value="local_model">本地模型编排 · 需要加载模型</option><option value="rule_fallback">规则编排 · 无需语言模型 GPU</option></select><p id="planner-mode-note" class="small gate-note">模型失败不会自动切换规则；请明确选择后再编排。</p></div>
<label for="instruction">自然语言指令</label><textarea id="instruction" rows="5" maxlength="2000" placeholder="例如：先向前走，然后向右看。修改时可以说：保持前进不变，只在第8到10秒抬头。"></textarea>
<p class="muted small">可描述移动、镜头方向、先后顺序、时间区间，也可缩短、延长或取消已有动作。时间按 16 FPS 换算为控制帧；控制时间正确不保证画面动作准确实现。修改时请说明保留什么、改变什么。</p>
<label for="edit-base-version">本次编辑基线</label><select id="edit-base-version" disabled><option value="">跟随最近可执行计划</option></select>
<p id="edit-base" class="small gate-note">请在此选择要修改的版本；下方对比选择和接受版本不会改变编辑基线。</p>
<div class="editor-actions"><span id="session-context" class="muted small">尚未保存 · 编排后建立会话</span><button id="plan" class="secondary" type="button" disabled>编排动作计划 →</button></div>
<div class="plan-block"><div class="plan-heading"><h3>本次编排 · 生成前核对计划</h3><span id="plan-status" class="status">尚未编排</span></div>
<div id="plan-empty" class="plan-empty"><span aria-hidden="true">↗</span><p>你的描述会转成具体按键与时长。<br>确认后再点击生成；编排不会自动提交视频任务。</p></div>
<div id="plan-content" hidden><p id="plan-explanation"></p><div id="plan-details" class="plan-details"></div><div id="action-segments" class="action-segments"></div><p id="plan-total" class="muted small"></p></div>
<div class="generate-row"><div><strong id="generation-label">生成 15 秒视频</strong><p id="generation-help" class="muted small">等待一份可执行的动作计划。</p></div><button id="generate" type="button" class="primary" disabled>确认并生成 →</button></div></div></section></div>
<section class="versions-section" aria-labelledby="versions-heading"><div class="section-heading"><div><div class="section-title"><span class="step">03</span><h2 id="versions-heading">回看、反馈与选择</h2></div><p class="muted small">默认对比最近两版，也可选择任意历史计划或结果。动作差异是输入对比，不代表画面已实现目标。视觉反馈修订默认关闭；启用后最多一次，仍需手动确认生成。</p></div><span id="queue-summary" class="muted small">正在读取任务状态…</span></div>
<div id="comparison-controls" class="comparison-controls" hidden><div class="comparison-selectors"><div><label for="compare-left">左侧版本</label><select id="compare-left"></select></div><div><label for="compare-right">右侧版本</label><select id="compare-right"></select></div></div><div class="comparison-actions"><button id="compare-latest" type="button">回到最近两版</button><button id="download-session" type="button">↓ 会话 JSON</button><span id="comparison-mode" class="muted small"></span></div><div class="playback-actions"><button id="play-together" type="button" disabled>对齐并播放两版</button><button id="restart-together" type="button" disabled>从头播放两版</button><button id="pause-together" type="button" disabled>暂停两版</button><span id="playback-clock" class="muted small"></span></div><p class="muted small comparison-help">对齐以左侧当前时间为准；联动仅在点击时执行，不改变单个播放器操作。浏览器播放可能漂移，不是逐帧视觉评测。切换版本会暂停离开的播放器，轮询不会重建视频。</p></div>
<div id="selected-input-diff" class="input-diff selected-input-diff" aria-live="polite" hidden></div><p id="visual-gate" class="gate-note small"></p><p id="accepted-note" class="accepted-note" hidden></p><p id="empty-versions" class="empty">还没有版本。编排第一份动作计划，开始这次创作。</p><div id="versions" class="versions"></div></section>
<footer><span>InterActWorld Creator</span><p>保留真实原始输出，不用示例视频替代失败结果。视觉观察与规则判断都可能有误，请结合视频审核；未配置视觉观察时使用人工审核。</p></footer>
</main><script src="/app.js"></script></body></html>'''

CSS = '''
*{box-sizing:border-box}body{margin:0;background:#090f18;color:#e8eef6;font:15px/1.65 system-ui,-apple-system,"Segoe UI",sans-serif}main{max-width:1240px;margin:auto;padding:30px 30px 18px}button,input,select,textarea{font:inherit;color:inherit;border:1px solid #35485c;border-radius:8px;background:#162334;padding:9px 12px}button{cursor:pointer;font-size:13px;font-weight:600}button:hover:not(:disabled){border-color:#9ee6d2;background:#203346}button:disabled{opacity:.43;cursor:not-allowed}input:focus,select:focus,textarea:focus,button:focus-visible,a:focus-visible,summary:focus-visible{outline:2px solid #a7ebda;outline-offset:3px}textarea{resize:vertical;line-height:1.7}label{display:block;font-size:12px;color:#adbdcd;margin-bottom:7px}select{max-width:100%}h1,h2,h3,p{margin-top:0}h1{font-size:clamp(30px,4.5vw,49px);font-weight:660;letter-spacing:-1.5px;line-height:1.25;margin:13px 0 15px}h1 em{font-style:normal;color:#a7ebda}h2{font-size:17px;line-height:1.5;margin:0}h3{font-size:15px;margin:0}.brand{display:flex;align-items:center;gap:12px;color:#c4d2df;letter-spacing:1.6px;font-size:12px;margin-bottom:42px}.brand b{font-weight:650;color:#a7ebda;margin-left:6px}.brand-mark{padding:3px 7px;border:1px solid #48665d;border-radius:6px;color:#a7ebda;font:700 14px/1.5 monospace}.brand a{margin-left:auto;color:#93a9bb;text-decoration:none;letter-spacing:0}.eyebrow{font-size:11px;letter-spacing:2px;color:#87abaf}.lede{color:#9eafbf;font-size:15px;margin-bottom:18px}.badges,.capabilities{display:flex;gap:8px;flex-wrap:wrap}.badges span{font-size:11px;color:#a9bccc;border:1px solid #2c3d4e;border-radius:20px;padding:4px 10px}.capabilities{font-size:11px;color:#88a4b5;margin-top:14px;gap:18px}.workspace-heading{display:flex;justify-content:space-between;align-items:center;gap:16px;margin:36px 0 16px}.workspace-heading p{margin:4px 0 0}.session-controls{display:flex;align-items:center;gap:9px}.session-controls select{width:265px;font-size:12px}.session-controls button{white-space:nowrap}.compose-grid{display:grid;grid-template-columns:300px minmax(0,1fr);gap:18px}section{border:1px solid #273848;border-radius:14px;background:#111b28;padding:22px}.section-title{display:flex;align-items:center;gap:10px;margin-bottom:19px}.step{font:600 11px/1.5 monospace;color:#92b8b3;border:1px solid #334e52;border-radius:5px;padding:2px 6px}.scene-panel select,.scene-panel input{width:100%;font-size:12px}.initial-wrap{position:relative;margin:17px 0 20px;border:1px solid #2d3d4d;border-radius:8px;overflow:hidden;background:#070c13}#initial{width:100%;aspect-ratio:832/480;display:block;object-fit:contain}.image-caption{display:block;border-top:1px solid #2d3d4d;padding:6px 9px;font-size:9px;letter-spacing:1.1px;color:#95abba;background:#0e1721}#prompt{width:100%;min-height:134px;max-height:350px;padding:11px;color:#b3c3d1;background:#0c1520;border-color:#263b4c;font-size:12px}details{margin-top:16px;color:#94aabc}summary{cursor:pointer;font-size:11px}details p{margin:9px 0 0;overflow-wrap:anywhere}.muted{color:#92a6b9}.small{font-size:12px}.scene-panel .small{font-size:11px;line-height:1.8}.composer{min-width:0}#instruction{display:block;width:100%;min-height:132px;background:#0d1825;border-color:#3b5267;padding:13px 15px;font-size:14px}#instruction::placeholder{color:#7790a6}.composer>p{margin:9px 0 13px}.editor-actions{display:flex;align-items:center;justify-content:space-between;gap:12px}.secondary{color:#b8eddf;border-color:#4b756f;background:#16342f;padding:10px 16px}.plan-block{margin-top:22px;padding-top:20px;border-top:1px solid #283c4b}.plan-heading{display:flex;gap:12px;align-items:center;justify-content:space-between}.status{display:inline-block;border-radius:18px;padding:3px 9px;background:#24384c;color:#b6cfe2;white-space:nowrap;font-size:11px}.status.ready,.status.completed,.status.satisfied{color:#a7ebda;background:#1e3d36}.status.failed,.status.unsatisfied,.status.unsupported{color:#f0b6af;background:#452e34}.status.clarify,.status.uncertain{color:#e8c997;background:#443b2a}.plan-empty{display:flex;align-items:center;justify-content:center;gap:16px;min-height:130px;color:#839aaf;font-size:13px}.plan-empty>span{font-size:32px;color:#648d88}.plan-empty p{margin:0}#plan-explanation{font-size:13px;color:#c4d1dd;margin:14px 0;white-space:pre-wrap}.plan-details{display:grid;gap:6px;margin-bottom:14px}.detail-row{font-size:12px;color:#a9bfce;overflow-wrap:anywhere}.detail-row b{font-weight:500;color:#718fa5;margin-right:10px}.action-segments{display:grid;gap:7px;max-height:290px;overflow:auto}.action-segment{display:grid;grid-template-columns:98px minmax(0,1fr) auto;align-items:center;gap:10px;padding:10px 12px;border-radius:7px;background:#0d1722;border:1px solid #283c4a;font-size:12px}.segment-time{font-variant-numeric:tabular-nums;color:#99afc2}.key-set{display:flex;gap:4px;flex-wrap:wrap}kbd{display:inline-block;background:#1d353c;color:#c0efe3;border:1px solid #456168;box-shadow:0 1px 0 #456168;border-radius:4px;font:11px/1.3 monospace;padding:3px 6px}.segment-frames{color:#7894a8;font-size:11px}#plan-total{margin:9px 0 0}.generate-row{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-top:20px;padding-top:18px;border-top:1px solid #283c4b}.generate-row strong{font-size:13px}.generate-row p{margin:3px 0 0;font-size:11px;max-width:340px}.primary{background:#a7ebda;border-color:#a7ebda;color:#0c2828;padding:12px 20px;white-space:nowrap}.primary:hover:not(:disabled){background:#c0f6e7;border-color:#c0f6e7}.notice{margin:22px 0 0;padding:12px 15px;background:#193129;border:1px solid #3b6052;color:#b9e4d3;border-radius:8px;font-size:13px;white-space:pre-wrap;overflow-wrap:anywhere}.notice.error{background:#2e2029;border-color:#61414c;color:#edb6b6}.versions-section{margin-top:20px}.section-heading{display:flex;align-items:start;justify-content:space-between;gap:16px}.section-heading .section-title{margin-bottom:7px}.section-heading p{margin-bottom:18px}.section-heading>span{padding-top:5px;white-space:nowrap}.empty{padding:32px 16px;border:1px dashed #344659;border-radius:8px;color:#839caf;text-align:center;font-size:13px;margin:2px 0 0}.versions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.version{min-width:0;background:#0b1521;border:1px solid #2e4353;border-radius:10px;overflow:hidden}.version.selected{border-color:#729e8e}.version-top{padding:15px 16px 12px}.version-head{display:flex;align-items:center;gap:9px;justify-content:space-between}.version-title{font-weight:650;font-size:13px}.version-meta{color:#839aaf;font-size:11px;margin:7px 0 0;overflow-wrap:anywhere}.version-text{font-size:12px;color:#b5c6d6;white-space:pre-wrap;overflow-wrap:anywhere;margin:9px 0 0;max-height:120px;overflow:auto}.video-wrap{background:#050b11;border-block:1px solid #263c4c;aspect-ratio:832/480;display:flex;align-items:center;justify-content:center}.video-wrap video{width:100%;height:100%;max-height:420px;display:block;object-fit:contain}.video-placeholder{text-align:center;padding:22px;color:#8fa9b8;font-size:12px}.video-placeholder p{margin:5px 0}.video-placeholder strong{display:block;font-weight:550;color:#b5c8d6}.video-placeholder progress{width:170px;max-width:100%;height:5px;accent-color:#a7ebda}.job-error{font-size:12px;white-space:pre-wrap;overflow-wrap:anywhere;color:#f0b6af;background:#302129;padding:10px 16px;margin:0}.version-body{padding:13px 16px 16px}.downloads{display:flex;gap:13px;flex-wrap:wrap;font-size:11px;min-height:20px}.downloads a{color:#a7cdbf;text-decoration:none}.downloads a:hover{text-decoration:underline}.review-summary{border-top:1px solid #263c4c;margin-top:13px;padding-top:12px;font-size:12px}.review-summary p{margin:5px 0;color:#98adbe;white-space:pre-wrap;overflow-wrap:anywhere}.review-summary strong{font-weight:500;color:#c0d7cb}.review-form{margin-top:13px;border-top:1px solid #263c4c;padding-top:12px}.review-form label{font-size:11px}.review-form select{font-size:12px;width:100%;padding:7px 10px}.review-form textarea{display:block;width:100%;margin:8px 0 10px;font-size:12px;min-height:75px;background:#111e2c}.review-actions,.version-actions{display:flex;gap:7px;flex-wrap:wrap}.review-actions button,.version-actions button{font-size:11px;padding:7px 10px}.version-actions{margin-top:9px}.accept{color:#a7ebda;border-color:#436b60}.accepted-note{color:#a7ebda;background:#19352e;padding:9px 12px;border-radius:6px;font-size:12px;margin-bottom:15px}.sr-only{position:absolute;width:1px;height:1px;padding:0;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}footer{display:flex;gap:26px;align-items:start;margin-top:27px;color:#7e94a7;font-size:11px}footer>span{white-space:nowrap;color:#9cb6be}footer p{max-width:810px;margin:0 0 15px}[hidden]{display:none!important}@media(max-width:980px){.compose-grid{grid-template-columns:245px minmax(0,1fr)}section{padding:18px}.session-controls select{width:210px}.generate-row{align-items:start;flex-direction:column}.generate-row .primary{width:100%}}@media(max-width:720px){main{padding:20px 13px}.brand{margin-bottom:31px}.brand a{display:none}.workspace-heading{align-items:stretch;flex-direction:column;margin-top:28px}.session-controls select{flex:1;width:auto;min-width:0}.compose-grid{grid-template-columns:minmax(0,1fr)}.scene-panel{display:grid;grid-template-columns:1fr}.initial-wrap{max-width:480px;width:100%;margin:14px auto}#prompt{min-height:90px}.versions{grid-template-columns:minmax(0,1fr)}.section-heading{flex-direction:column;gap:0}.section-heading>span{margin-bottom:15px}.editor-actions{align-items:stretch;flex-direction:column}.action-segment{grid-template-columns:85px minmax(0,1fr) auto;padding:9px 8px;gap:6px}.brand{font-size:11px}footer{display:block}footer p{margin-top:7px}h1{letter-spacing:-1px}.plan-heading{align-items:start}}
/* Comparison controls and persistent per-version media. */
.selected-input-diff{padding:13px 15px;border:1px solid #2e4353;border-radius:8px;background:#0b1521;margin:0 0 15px}.selected-input-diff p{margin:5px 0}.selected-input-diff .diff-table-wrap{margin:9px 0}
.version[data-side="left"]{grid-column:1}.version[data-side="right"]{grid-column:2}@media(max-width:720px){.version[data-side]{grid-column:1}}
.comparison-controls{border:1px solid #2e4353;border-radius:9px;padding:14px;margin-bottom:15px;background:#0b1521}.comparison-selectors{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:15px}.comparison-selectors>div{min-width:0}.comparison-selectors select{width:100%;font-size:12px}.comparison-actions,.playback-actions{display:flex;align-items:center;flex-wrap:wrap;gap:8px;margin-top:12px}.comparison-actions button,.playback-actions button{font-size:11px;padding:7px 10px}.comparison-help{margin:9px 0 0;font-size:11px}.gate-note{color:#dbc795;overflow-wrap:anywhere}.media-controls{display:flex;align-items:center;flex-wrap:wrap;gap:9px;padding:9px 16px;background:#101d29}.media-controls label{margin:0}.media-controls select{padding:5px 8px;font-size:11px}.media-error{color:#efb7ad;font-size:11px;overflow-wrap:anywhere}.downloads button{font-size:11px;padding:3px 7px}.version-inputs{border-top:1px solid #263c4c;padding-top:12px}.version-inputs summary{font-size:12px;color:#c0d7cb}.version-inputs .action-segments{margin-top:12px}.version-inputs .action-segment{grid-template-columns:92px minmax(0,1fr) auto;font-size:11px;padding:8px;gap:5px}.input-note{font-size:11px;line-height:1.7}.input-diff{margin-top:13px}.input-diff h4{font-size:12px;font-weight:550;color:#bcd7ce;margin:0 0 7px}.diff-table-wrap{overflow:auto}.diff-table{border-collapse:collapse;width:100%;font-size:11px;text-align:left;white-space:nowrap}.diff-table th,.diff-table td{padding:5px 6px;border-bottom:1px solid #273b4a}.diff-table th{font-weight:500;color:#87a5b9}.diff-changed{color:#e9cc9f}.review-provenance{padding:8px 10px;background:#152532;border-radius:5px}.review-summary pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:11px;max-height:260px;overflow:auto}.version{align-self:start}.version.selected .version-title{color:#a7ebda}.accept-explanation{margin:10px 0 0;font-size:11px}@media(max-width:720px){.comparison-selectors{grid-template-columns:minmax(0,1fr);gap:10px}.comparison-actions,.playback-actions{align-items:stretch}.playback-actions button{flex:1}.comparison-actions span,.playback-actions span{width:100%}.version-inputs .action-segment{grid-template-columns:83px minmax(0,1fr) auto}}
'''

CSS += '''
.review-source-panel{border:1px solid #304554;border-radius:7px;padding:10px 11px;margin-bottom:10px}.review-source-panel h4{margin:0 0 8px;font-size:12px;color:#c5dfd6}.human-review-panel{border-left:3px solid #9ed9c7}.model-review-panel{border-left:3px solid #b7a0d0}.model-review-panel h4{color:#d2bfe6}.review-criteria{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin:12px 0}.review-criteria label{font-size:10px}.review-criteria select{padding:6px;font-size:11px}.historical-review{border-top:1px dashed #344958;padding-top:10px;margin-top:10px}.review-form>.small{line-height:1.7;font-size:10px}.review-form>label:not(:first-child){margin-top:12px}.review-form .review-edit{min-height:62px}@media(max-width:720px){.review-criteria{grid-template-columns:minmax(0,1fr);gap:8px}}
'''

JS = r''''use strict';
const $ = id => document.getElementById(id);
const JOB_STATUS = {queued:'等待生成',running:'正在生成',completed:'生成完成',failed:'生成失败',cancelled:'已取消',unavailable:'任务记录不可用'};
const PLAN_STATUS = {ready:'可执行',clarify:'需要澄清',unsupported:'暂不支持'};
const VERDICT = {satisfied:'满意',unsatisfied:'不满意',uncertain:'不确定'};
const DECISION = {accept:'建议接受',revise:'建议修订',ask_user:'需要人工确认',stop:'停止自动修订'};
const REVIEW_CRITERIA = {movement_response:'移动响应',camera_response:'镜头响应',temporal_stability:'连续性与形变'};
const KEY_LABELS = {W:'W 前进',A:'A 左移',S:'S 后退',D:'D 右移',I:'↑ 抬头',J:'← 左看',K:'↓ 低头',L:'→ 右看'};
let config=null, sessions=[], jobsById=new Map(), selectedId='', busy=false, refreshing=false, planSignature='';
const requestIds=new Map(), cards=new Map(), reviewDrafts=new Map(), comparisons=new Map(), editBases=new Map(), plannedVersions=new Map();
function el(tag, cls, text){const node=document.createElement(tag);if(cls)node.className=cls;if(text!==undefined)node.textContent=text;return node}
function describe(value){if(value===null||value===undefined)return '';if(typeof value==='string')return value;if(Array.isArray(value))return value.map(describe).join('；');return JSON.stringify(value)}
function storageGet(key){try{return localStorage.getItem(key)||''}catch(_){return ''}}
function storageSet(key,value){try{localStorage.setItem(key,value)}catch(_){}}
function message(text,error=false){$('notice').textContent=text;$('notice').hidden=!text;$('notice').className='notice'+(error?' error':'')}
function safeURL(value){try{const url=new URL(value,location.origin);return url.origin===location.origin&&['http:','https:'].includes(url.protocol)?url.href:''}catch(_){return ''}}
function session(){return sessions.find(s=>s.session_id===selectedId)||null}
function versions(){return session()?.versions||[]}
function latest(){return versions().at(-1)||null}
function editBaseSelection(){if(!selectedId)return '';if(!editBases.has(selectedId))editBases.set(selectedId,storageGet('interactworld.creator.edit-base.'+selectedId));const id=editBases.get(selectedId)||'';if(id&&!versions().some(v=>v.version_id===id&&v.plan?.status==='ready')){editBases.set(selectedId,'');storageSet('interactworld.creator.edit-base.'+selectedId,'');return ''}return id}
function editBaseVersion(){const id=editBaseSelection();return (id?versions().find(v=>v.version_id===id):versions().findLast(v=>v.plan?.status==='ready'))||null}
function setEditBase(id){if(!selectedId||typeof id!=='string'||(id&&!versions().some(v=>v.version_id===id&&v.plan?.status==='ready')))return false;editBases.set(selectedId,id);storageSet('interactworld.creator.edit-base.'+selectedId,id);return true}
function plannedVersion(){if(!selectedId)return null;if(!plannedVersions.has(selectedId))plannedVersions.set(selectedId,storageGet('interactworld.creator.planned-version.'+selectedId));const id=plannedVersions.get(selectedId);return versions().find(v=>v.version_id===id)||latest()}
function rememberPlannedVersion(id){if(typeof id==='string'&&versions().some(v=>v.version_id===id)){plannedVersions.set(selectedId,id);storageSet('interactworld.creator.planned-version.'+selectedId,id)}}
function planPayload(){const body={scene_id:$('scene').value,seed:Number($('seed').value),text:$('instruction').value.trim()};if(config.rule_planner_available)body.planner=$('planner-mode').value;if(selectedId){body.session_id=selectedId;const base=editBaseVersion();if(base)body.base_version_id=base.version_id}return body}
function scene(){return config?.scenes.find(s=>s.scene_id===$('scene').value)}
function jobFor(v){return jobsById.get(v.job_id)||null}
function jobStatus(v){return jobFor(v)?.status||v.job_status||''}
function numberLabel(n){return Number.isInteger(n)?String(n):Number(n).toFixed(2)}
function timeLabel(value){if(!value)return '';const date=new Date(typeof value==='number'?value*1000:value);return Number.isNaN(date.getTime())?String(value):date.toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'})}
function plannerLabel(kind){if(!kind)return '编排器：未提供来源';if(kind==='rule_fallback')return '编排器：规则回退（非 LLM）';if(kind==='external_proposal_validated')return '编排器：外部提案 · 已通过规则校验';return '编排器：'+String(kind)}
function initializePlannerMode(){const ruleOnly=config.planner_kind==='rule_fallback';$('planner-mode').value=ruleOnly?'rule_fallback':'local_model';$('planner-mode').querySelector('option[value="local_model"]').disabled=ruleOnly}
function hasObserver(){return !!config?.observer_kind&&!['none','manual','human','human_only','manual_only','unconfigured','disabled','rule_fallback'].includes(config.observer_kind)}
function observerLabel(){return hasObserver()?'视觉观察：'+config.observer_kind+(config.visual_revision_enabled===true?' · 未校准，结果需人工确认':' · 未校准，人工审核（视觉修订关闭）'):'视觉观察：未配置 · 人工审核'}
async function api(path,body){const options={cache:'no-store'};if(body!==undefined){options.method='POST';options.headers={'Content-Type':'application/json','X-InterActWorld-CSRF':config.csrf_token};options.body=JSON.stringify(body)}
  const response=await fetch(path,options);let data;try{data=await response.json()}catch(_){throw Error('服务未返回有效 JSON（HTTP '+response.status+'）')}
  if(!response.ok)throw Error(describe(data.error||data.detail)||'请求失败（HTTP '+response.status+'）');return data;
}
function requestId(operation,body){const key=operation+JSON.stringify(body);if(!requestIds.has(key)){const id=typeof crypto.randomUUID==='function'?crypto.randomUUID():Date.now().toString(36)+'-'+Math.random().toString(36).slice(2);requestIds.set(key,id)}return {key,id:requestIds.get(key)}}
function upsertSession(data){const saved=data.session||data;if(!saved.session_id)throw Error('服务响应缺少会话标识');const index=sessions.findIndex(s=>s.session_id===saved.session_id);if(index<0)sessions.unshift(saved);else sessions[index]=saved;selectedId=saved.session_id;storageSet('interactworld.creator.session',selectedId);rememberPlannedVersion(data.planned_version_id||saved.planned_version_id)}
function validateEditor(){const seed=Number($('seed').value);if(!$('instruction').value.trim())return '请先填写动作描述。';if(!scene())return '请选择一个有效场景。';if($('seed').value===''||!Number.isInteger(seed)||seed<0||seed>4294967295)return '随机种子必须是 0–4294967295 的整数。';return ''}
function canGenerate(v){return !!v&&v.plan?.status==='ready'&&Array.isArray(v.plan.action_segments)&&v.plan.action_segments.length>0&&!v.job_id&&!!config?.generation_enabled}
function updateControls(){const saved=session(),v=plannedVersion();$('scene').disabled=busy||!config||!!saved;$('seed').disabled=busy||!config||!!saved;$('session').disabled=busy||!config;$('new-session').disabled=busy||!config;$('instruction').disabled=busy||!config;$('edit-base-version').disabled=busy||!versions().some(item=>item.plan?.status==='ready');
  $('planner-mode-control').hidden=!config?.rule_planner_available;$('planner-mode').disabled=busy||!config?.rule_planner_available;$('planner-mode-note').textContent=$('planner-mode').value==='rule_fallback'?'规则编排仅支持已实现的方向、顺序和局部时间修改；不理解任意开放目标，也不是 LLM 推理。视频生成仍需 GPU 与预算授权。':'模型失败不会自动切换规则；需要时可手动改为规则编排，不影响已保存计划。';
  $('plan').disabled=busy||!config||!!validateEditor();$('plan').textContent=busy?'正在处理…':saved?'编排新版本 →':'编排动作计划 →';$('generate').disabled=busy||!canGenerate(v);
  let hint='等待一份可执行的动作计划。';if(config&&!config.generation_enabled)hint='预览模式：生成尚未启用；可先编排与检查计划。';else if(v?.job_id)hint='这一版已提交任务。请在下方查看结果；修改描述可创建新版本。';else if(v?.plan?.status==='ready')hint='确认后将提交真实模型任务，排队与生成需要时间。';else if(v)hint='请根据计划说明调整描述，再编排一个新版本。';$('generation-help').textContent=hint;
  for(const card of cards.values())for(const button of card.querySelectorAll('button[data-action]'))button.disabled=busy||button.dataset.allowed!=='yes';
}
function renderSessionOptions(){const select=$('session');select.replaceChildren();const fresh=el('option','','新会话');fresh.value='';select.append(fresh);for(const s of sessions){const option=el('option','',`${s.scene_id} · ${timeLabel(s.created_at)||String(s.session_id).slice(0,8)} · ${(s.versions||[]).length} 版`);option.value=s.session_id;select.append(option)}select.value=selectedId;
  $('session-context').textContent=session()?`会话 ${selectedId.slice(0,8)} · seed ${session().seed} · 已保存`:'尚未保存 · 编排后建立会话';
  renderEditBase();
}
function renderEditBase(){const select=$('edit-base-version'),all=versions(),base=editBaseVersion(),selected=editBaseSelection(),signature=JSON.stringify([selectedId,all.map(v=>[v.version_id,v.plan?.status])]);if(select.dataset.signature!==signature){select.dataset.signature=signature;select.replaceChildren();const follow=el('option','','跟随最近可执行计划');follow.value='';select.append(follow);for(const [index,v] of all.entries())if(v.plan?.status==='ready'){const option=el('option','',`固定版本 ${index+1} · ${String(v.version_id).slice(0,8)}`);option.value=v.version_id;select.append(option)}}select.value=selected;const index=base?all.findIndex(v=>v.version_id===base.version_id):-1;$('edit-base').textContent=(base?`${selected?'已固定':'动态跟随'}：本次修改基于版本 ${index+1}（${String(base.version_id).slice(0,8)}），不要求此版已生成视频。`:'当前无可执行父计划，新描述将编排首份动作计划。')+'下方历史对比、接受版本均不会改变基线；文字中的版本名不会自动替代此选择。'}
function renderScene(){const s=scene();if(!s)return;$('initial').src=safeURL(s.initial_url);$('prompt').value=s.prompt||'';$('source').textContent=[`场景 ${s.scene_id}`,s.source_episode_id?'来源 '+s.source_episode_id:'',s.initial_sha256?'首图 SHA '+s.initial_sha256:'',s.method?'生成方式 '+s.method:''].filter(Boolean).join(' · ')}
function alignSessionInputs(){const saved=session();if(saved){$('scene').value=saved.scene_id;$('seed').value=saved.seed}renderScene()}
function addDetail(parent,label,value){if(value===null||value===undefined||value===''||(Array.isArray(value)&&!value.length))return;const row=el('div','detail-row');row.append(el('b','',label),el('span','',describe(value)));parent.append(row)}
function renderPlan(){const v=plannedVersion(),signature=JSON.stringify([selectedId,v?.version_id,v?.base_version_id,v?.parent_version,v?.plan]);if(signature===planSignature)return;planSignature=signature;
  $('plan-empty').hidden=!!v;$('plan-content').hidden=!v;$('plan-status').className='status';$('plan-status').textContent=v?(PLAN_STATUS[v.plan?.status]||v.plan?.status||'未提供状态'):'尚未编排';if(!v)return;
  const p=v.plan||{};$('plan-status').className='status '+(['ready','clarify','unsupported'].includes(p.status)?p.status:'');$('plan-explanation').textContent=describe(p.explanation)||'未提供编排说明。';
  const detail=$('plan-details');detail.replaceChildren();addDetail(detail,'计划版本',`${versions().findIndex(item=>item.version_id===v.version_id)+1} · ${String(v.version_id).slice(0,8)}`);addDetail(detail,'实际编辑基线',v.base_version_id||v.parent_version||'首版，无父计划');addDetail(detail,'目标',p.goals);addDetail(detail,'修改范围',({all:'整个动作时间线',movement:'仅移动输入',camera:'仅镜头输入'})[p.edit_scope]||p.edit_scope);addDetail(detail,'保留检查',p.preserved===true?'编辑范围之外的输入已逐帧校验':p.preserved===false?'首版或全量修改不适用；局部修改请核对计划说明':p.preserved);addDetail(detail,'编排来源',plannerLabel(p.planner_kind||config.planner_kind));if(v.provider||p.provider)addDetail(detail,'提供方',v.provider||p.provider);if(p.edit_patch&&typeof p.edit_patch==='object')addDetail(detail,'结构化编辑 Patch',p.edit_patch);
  const list=$('action-segments');list.replaceChildren();let offset=0;for(const [index,segment] of (Array.isArray(p.action_segments)?p.action_segments:[]).entries()){const frames=Number(segment.frames)||0,seconds=frames/16,row=el('div','action-segment');row.append(el('span','segment-time',`${numberLabel(offset)}–${numberLabel(offset+seconds)} 秒`));const keys=el('span','key-set');for(const key of (Array.isArray(segment.keys)?segment.keys:[]))keys.append(el('kbd','',KEY_LABELS[key]||String(key)));if(!keys.childNodes.length)keys.append(el('span','muted','保持'));row.append(keys,el('span','segment-frames',`${frames} 帧`));row.setAttribute('aria-label',`第 ${index+1} 段，${seconds} 秒`);list.append(row);offset+=seconds}
  $('plan-total').textContent=list.childNodes.length?`${list.childNodes.length} 段 · 合计 ${numberLabel(offset)} 秒 · 16 帧/秒 · 方向键对应模型 I/J/K/L 输入`:'此计划没有可执行动作段。';
}
function reviewEvidence(value){if(Array.isArray(value))return value.map(item=>typeof item==='object'&&item!==null?`${item.video==='reference'?'参考视频':'当前视频'} · ${item.time_seconds!==undefined?item.time_seconds+' 秒 · ':''}${describe(item.observation||item.evidence||item)}`:'当前视频 · '+describe(item)).join('\n');return describe(value)}
function reviewRecords(version){const records=[],seen=new Set();function add(record){if(!record||typeof record!=='object')return;if(record.previous_assessment)add(record.previous_assessment);const copy={...record};delete copy.previous_assessment;const key=JSON.stringify(copy);if(!seen.has(key)){seen.add(key);records.push(copy)}}for(const record of version.review_history||[])add(record);add(version.review);return records}
function reviewDraftKey(versionId){return 'interactworld.creator.review.'+selectedId+'.'+versionId}
function getReviewDraft(versionId){if(reviewDrafts.has(versionId))return reviewDrafts.get(versionId);try{const draft=JSON.parse(storageGet(reviewDraftKey(versionId)));if(draft&&typeof draft==='object'&&typeof draft.evidence==='string'){reviewDrafts.set(versionId,draft);return draft}}catch(_){}return null}
function rememberReviewDraft(versionId,draft){reviewDrafts.set(versionId,draft);storageSet(reviewDraftKey(versionId),JSON.stringify(draft))}
function useReviewEdit(versionId,text){if(busy)return;const instruction=typeof text==='string'?text.trim():'';if(!instruction){message('请先填写具体修改指令，例如“保持前进不变，只在第8到10秒抬头”。观察依据不会自动变成执行动作。',true);return}if(!setEditBase(versionId)){message('所选版本不是当前会话的可执行计划，无法作为修改基线。',true);return}$('instruction').value=instruction;renderEditBase();updateControls();$('instruction').focus();$('instruction').scrollIntoView({behavior:'smooth',block:'center'});message('修改指令已带入，编辑基线已固定为此历史版本。尚未调用模型、编排或提交生成；请核对后点击“编排新版本”。')}
function comparison(){const all=versions();let chosen=comparisons.get(selectedId);if(!chosen){try{chosen=JSON.parse(storageGet('interactworld.creator.compare.'+selectedId))}catch(_){}if(!chosen||typeof chosen!=='object')chosen={follow:true};comparisons.set(selectedId,chosen)}
  const defaults=all.slice(-2);if(chosen.follow!==false){chosen.left=defaults[0]?.version_id||'';chosen.right=defaults[1]?.version_id||''}else{if(chosen.left!==''&&!all.some(v=>v.version_id===chosen.left))chosen.left=defaults[0]?.version_id||'';if(!all.some(v=>v.version_id===chosen.right)||chosen.right===chosen.left)chosen.right=''}return chosen;
}
function saveComparison(){storageSet('interactworld.creator.compare.'+selectedId,JSON.stringify(comparison()))}
function chooseComparison(side,id){const chosen=comparison(),other=side==='left'?'right':'left',old=chosen[side];chosen.follow=false;chosen[side]=id;if(id&&chosen[other]===id)chosen[other]=old;saveComparison();renderVersions()}
function renderComparisonControls(){const all=versions(),chosen=comparison();$('comparison-controls').hidden=!all.length;for(const side of ['left','right']){const select=$('compare-'+side),signature=JSON.stringify(all.map(v=>[v.version_id,jobStatus(v),v.plan?.status]));if(select.dataset.signature!==signature){select.dataset.signature=signature;select.replaceChildren();const empty=el('option','','不展示');empty.value='';select.append(empty);for(const [index,v] of all.entries()){const option=el('option','',`版本 ${index+1} · ${JOB_STATUS[jobStatus(v)]||PLAN_STATUS[v.plan?.status]||'计划'} · ${String(v.version_id).slice(0,8)}`);option.value=v.version_id;select.append(option)}}select.value=chosen[side]||''}
  $('comparison-mode').textContent=chosen.follow!==false?'跟随最近两版':'已固定所选历史版本';$('visual-gate').textContent=config?.visual_revision_enabled===true?'视觉修订门：操作方已启用；未校准观察仍需规则校验与人工确认，不会自动生成。':'视觉修订门：关闭 · 未校准，降级为人工审核。参考视频与规则校验不保证模型判断可靠。';renderSelectedInputDiff();
}
function downloadJSON(name,data){const blob=new Blob([JSON.stringify(data,null,2)+'\n'],{type:'application/json;charset=utf-8'}),url=URL.createObjectURL(blob),link=el('a');link.href=url;link.download=name.replace(/[^a-zA-Z0-9._-]/g,'_');document.body.append(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),30000)}
function renderTimeline(parent,plan){parent.replaceChildren();let offset=0;for(const segment of Array.isArray(plan?.action_segments)?plan.action_segments:[]){const frames=Number(segment.frames)||0,row=el('div','action-segment'),keys=el('span','key-set');row.append(el('span','segment-time',`${numberLabel(offset/16)}–${numberLabel((offset+frames)/16)} 秒`));for(const key of Array.isArray(segment.keys)?segment.keys:[])keys.append(el('kbd','',KEY_LABELS[key]||String(key)));if(!keys.childNodes.length)keys.append(el('span','muted','保持'));row.append(keys,el('span','segment-frames',`${frames} 帧`));row.title=`动作输入帧 ${offset}–${offset+frames-1}（从 0 计）`;parent.append(row);offset+=frames}return offset}
function expandedInputs(plan){if(plan?.status!=='ready'||!Array.isArray(plan.action_segments))return null;const rows=[];for(const segment of plan.action_segments){if(!Number.isInteger(segment.frames)||segment.frames<1||segment.frames>240||!Array.isArray(segment.keys)||segment.keys.some(key=>!Object.hasOwn(KEY_LABELS,key))||new Set(segment.keys).size!==segment.keys.length)return null;const keys=segment.keys.slice().sort().join('');for(let i=0;i<segment.frames;i++)rows.push(keys);if(rows.length>240)return null}return rows.length===240?rows:null}
function compareInputs(before,after){const left=expandedInputs(before),right=expandedInputs(after);if(!left||!right)return null;return {changed:left.filter((keys,i)=>keys!==right[i]).length,keys:Object.keys(KEY_LABELS).map(key=>({key,before:left.filter(keys=>keys.includes(key)).length,after:right.filter(keys=>keys.includes(key)).length,added:right.filter((keys,i)=>keys.includes(key)&&!left[i].includes(key)).length,removed:left.filter((keys,i)=>keys.includes(key)&&!right[i].includes(key)).length}))}}
function renderSelectedInputDiff(){const all=versions(),chosen=comparison(),left=all.find(v=>v.version_id===chosen.left),right=all.find(v=>v.version_id===chosen.right),box=$('selected-input-diff'),signature=JSON.stringify([selectedId,chosen.left,chosen.right,left?.plan,right?.plan]);box.hidden=!all.length;if(box.dataset.signature===signature)return;box.dataset.signature=signature;box.replaceChildren();box.append(el('h4','',left&&right?`所选输入对比：左侧版本 ${all.indexOf(left)+1} → 右侧版本 ${all.indexOf(right)+1}`:'所选输入对比：左侧 → 右侧'));if(!left||!right){box.append(el('p','muted input-note','请选择左右两个版本以比较输入；各卡片仍单独保留真实父版信息。'));return}const diff=compareInputs(left.plan,right.plan);if(!diff){box.append(el('p','muted input-note','所选版本包含未就绪或不完整的计划，不能计算逐帧输入差异。'));return}
  box.append(el('p','input-note selected-diff-total',`${diff.changed} / 240 个动作帧的按键组合发生变化。此处比较所选计划，不要求两版互为父子；不代表视觉成功或画质改善。`));const changed=diff.keys.filter(item=>item.added||item.removed);if(changed.length){const wrap=el('div','diff-table-wrap'),table=el('table','diff-table'),head=el('thead'),heading=el('tr');for(const label of ['变化的按键','左侧 → 右侧','新增 / 移除帧']){const cell=el('th','',label);cell.scope='col';heading.append(cell)}head.append(heading);table.append(head);const body=el('tbody');for(const item of changed){const row=el('tr','diff-changed'),label=KEY_LABELS[item.key];row.append(el('td','',label.startsWith(item.key)?label:item.key+' / '+label),el('td','',`${item.before} → ${item.after} 帧`),el('td','',`+${item.added} / −${item.removed}`));body.append(row)}table.append(body);wrap.append(table);box.append(wrap)}else box.append(el('p','input-note','全部按键输入逐帧不变。'));if(diff.keys.filter(item=>'WASD'.includes(item.key)).every(item=>!item.added&&!item.removed))box.append(el('p','input-note selected-movement-preserved','移动输入 W/A/S/D：逐帧不变。'));box.append(el('p','muted input-note','新增/移除以左侧为基线、按帧位置计算；卡片中的“父版差异”仍按真实版本链计算。'));
}
function renderInputDetails(card,v){const parent=versions().find(item=>item.version_id===v.parent_version),signature=JSON.stringify([v.plan,parent?.version_id,parent?.plan]);if(card.dataset.inputSignature===signature)return;card.dataset.inputSignature=signature;const detail=card.querySelector('.version-inputs'),timeline=detail.querySelector('.action-segments'),frames=renderTimeline(timeline,v.plan);detail.querySelector('.timeline-note').textContent=frames?`完整动作输入：${frames} 帧 / ${numberLabel(frames/16)} 秒，16 帧/秒。按键与时间来自保存的计划，不是视觉成功证据。`:'本版没有可执行动作段；请阅读编排说明。';detail.querySelector('.version-plan-explanation').textContent=describe(v.plan?.explanation)||'未提供编排说明。';
  const diff=detail.querySelector('.input-diff');diff.replaceChildren();if(!v.parent_version){diff.append(el('p','muted input-note','首版，没有父版本可比较。'));return}const parentIndex=versions().indexOf(parent);diff.append(el('h4','',`与父版${parentIndex>=0?'（版本 '+(parentIndex+1)+'）':''}的按键输入差异`));const comparison=compareInputs(parent?.plan,v.plan);if(!comparison){diff.append(el('p','muted input-note','父版不可用或计划未就绪，不能计算逐帧输入差异。'));return}
  diff.append(el('p','input-note',`${comparison.changed} / 240 个动作帧的按键组合发生变化；仅比较输入，不评价画面。`));const wrap=el('div','diff-table-wrap'),table=el('table','diff-table'),head=el('thead'),row=el('tr');for(const label of ['按键','父版 → 本版','新增 / 移除帧'])row.append(el('th','',label));head.append(row);table.append(head);const body=el('tbody');for(const item of comparison.keys){const row=el('tr',item.added||item.removed?'diff-changed':'');row.append(el('td','',KEY_LABELS[item.key]),el('td','',`${item.before} → ${item.after} 帧`),el('td','',item.added||item.removed?`+${item.added} / −${item.removed}`:'逐帧不变'));body.append(row)}table.append(body);wrap.append(table);diff.append(wrap,el('p','muted input-note','新增/移除按逐帧位置计算；相同总帧数也可能改变动作时机。方向键对应 I/J/K/L 镜头输入。'));
}
function activeVideo(card){return card?.querySelector('video:not([hidden])')||null}
function comparisonVideos(){const chosen=comparison();return [chosen.left,chosen.right].map(id=>activeVideo(cards.get(id)))}
function updatePlaybackControls(){const players=comparisonVideos(),ready=players.every(video=>video&&video.readyState>=1&&!video.error);$('play-together').disabled=!ready;$('restart-together').disabled=!ready;$('pause-together').disabled=!players.some(Boolean);$('playback-clock').textContent=players.map((video,index)=>`${index?'右':'左'} ${video?numberLabel(video.currentTime||0)+' 秒':'无视频'}`).join(' · ')}
async function playTogether(restart){const players=comparisonVideos();if(!players.every(video=>video&&video.readyState>=1&&!video.error))return;const requested=restart?0:players[0].currentTime,target=Math.max(0,Math.min(requested,...players.map(video=>Number.isFinite(video.duration)?Math.max(0,video.duration-0.05):requested)));try{for(const video of players)video.currentTime=target;const results=await Promise.allSettled(players.map(video=>video.play()));const failed=results.find(result=>result.status==='rejected');if(failed){for(const video of players)video.pause();throw failed.reason}}catch(error){message('两版联动播放失败：'+error.message+'。可分别使用播放器控件。',true)}updatePlaybackControls()}
function ensureVideo(card,v,mode,seekTo){const media=card.querySelector('.video-wrap');let video=media.querySelector('video[data-mode="'+mode+'"]');if(!video){video=el('video');video.dataset.mode=mode;video.controls=true;video.playsInline=true;video.preload='metadata';video.setAttribute('aria-label',`${card.querySelector('.version-title').textContent} · ${mode==='raw'?'原始视频':'带输入栏视频'}`);video.onloadedmetadata=()=>{if(video.dataset.seekTo!==undefined){const target=Number(video.dataset.seekTo);if(Number.isFinite(target)&&target<video.duration)video.currentTime=target;delete video.dataset.seekTo}updatePlaybackControls()};video.ontimeupdate=updatePlaybackControls;video.onplay=updatePlaybackControls;video.onpause=updatePlaybackControls;video.onerror=()=>{card.querySelector('.media-error').textContent=(mode==='raw'?'原始视频':'带输入栏视频')+'读取失败；保留实际错误，不替换为示例。';updatePlaybackControls()};video.src=`/api/jobs/${encodeURIComponent(v.job_id)}/files/${mode==='raw'?'raw':'inputs'}.mp4`;media.append(video)}
  if(Number.isFinite(seekTo)){if(video.readyState>=1&&seekTo<video.duration)video.currentTime=seekTo;else video.dataset.seekTo=String(seekTo)}for(const player of media.querySelectorAll('video')){player.hidden=player!==video;if(player!==video)player.pause()}return video;
}
function renderReviewRecord(summary,review){const source=review.source==='human'?'人工审核':review.source==='model_assessment'?'模型视觉观察（未校准，非事实保证）':describe(review.source)||'未标注来源';summary.append(el('strong','',`${VERDICT[review.verdict]||review.verdict||'未判定'} · ${source}`));if(review.created_at)summary.append(el('p','muted',timeLabel(review.created_at)));if(review.source==='human'&&review.criteria){for(const [key,label] of Object.entries(REVIEW_CRITERIA)){if(review.criteria[key])summary.append(el('p','human-criterion',`${label}：${VERDICT[review.criteria[key]]||describe(review.criteria[key])}`))}}
  if(review.source==='model_assessment'){const reference=el('p','review-provenance');if(review.reference_used===true){const index=versions().findIndex(item=>item.version_id===review.reference_version_id);reference.textContent=`已提供参考视频：${index>=0?'版本 '+(index+1):describe(review.reference_version_id)||'版本未标注'}${review.reference_job_id?' · 任务 '+review.reference_job_id:''}。参考仅说明模型收到对照，不保证判断可靠。`}else reference.textContent=review.reference_used===false?'未使用参考视频；不能据此确认相对上一版的视觉改善。':'旧记录未注明是否使用参考视频；不推断对照已提供。';summary.append(reference)}
  const evidence=reviewEvidence(review.evidence);if(evidence)summary.append(el('p','review-evidence',evidence));if(review.decision)summary.append(el('p','effective-decision','有效处理：'+(DECISION[review.decision]||describe(review.decision))));if(review.downgrade_reason)summary.append(el('p','',describe(review.downgrade_reason)));if(review.rule_downgraded)summary.append(el('p','rule-gate','规则门：已降级，不能按原模型判断继续。'));if(Array.isArray(review.rule_reasons)&&review.rule_reasons.length)summary.append(el('p','rule-reasons',review.rule_reasons.map(describe).join('\n')));
  if(Array.isArray(review.goal_semantics)&&review.goal_semantics.length){const semantics=el('details');semantics.append(el('summary','','目标语义与所需证据（不表示证据已充分）'));for(const goal of review.goal_semantics){const categories=(Array.isArray(goal.categories)?goal.categories:[]).map(value=>({camera:'镜头',movement:'移动',relative:'相对变化',other:'其他'})[value]||String(value));semantics.append(el('p','',`${describe(goal.goal)} · ${categories.join(' / ')||'未分类'}${goal.requires_dense_temporal_evidence?' · 需要密集时序证据':''}`))}summary.append(semantics)}
  if(review.model_decision&&review.model_decision!==review.decision)summary.append(el('p','',`原模型决策（未执行）：${DECISION[review.model_decision]||describe(review.model_decision)}`));if(review.model_revision_text)summary.append(el('p','',`原修订建议（未验证）：${describe(review.model_revision_text)}`));if(review.original_model_judgment){const original=el('details');original.append(el('summary','','原模型判断（仅溯源，不作为有效结论）'),el('pre','',JSON.stringify(review.original_model_judgment,null,2)));summary.append(original)}if(review.confidence_note)summary.append(el('p','',describe(review.confidence_note)));if(review.observer_kind||review.provider)summary.append(el('p','',`观察来源：${describe(review.observer_kind||review.provider)}`));
}
function renderReviewSummary(summary,version){summary.replaceChildren();const records=reviewRecords(version);for(const [source,title,css,empty] of [['human','人工意见 · 由你填写','human-review-panel','尚未保存人工意见；不预填满意，也不采用模型结论代填。'],['model_assessment','VLM 观察 · 仅供参考','model-review-panel','尚无已返回的视觉观察；不可据此判断成功或失败。']]){const panel=el('div','review-source-panel '+css);panel.append(el('h4','',title));const matching=records.filter(record=>record.source===source),current=matching.at(-1);if(current)renderReviewRecord(panel,current);else panel.append(el('p','muted',empty));if(matching.length>1){const history=el('details','review-history');history.append(el('summary','',`较早的${source==='human'?'人工意见':'模型观察'}（${matching.length-1} 条）`));for(const record of matching.slice(0,-1).reverse()){const item=el('div','historical-review');renderReviewRecord(item,record);history.append(item)}panel.append(history)}summary.append(panel)}for(const record of records.filter(record=>!['human','model_assessment'].includes(record.source))){const unknown=el('details');unknown.append(el('summary','','其他来源的旧记录'));renderReviewRecord(unknown,record);summary.append(unknown)}}
function newAction(label,action,allowed){const button=el('button','',label);button.type='button';button.dataset.action=action;button.dataset.allowed=allowed?'yes':'no';button.disabled=busy||!allowed;return button}
function createCard(v){const card=el('article','version');card.dataset.versionId=v.version_id;card.dataset.sessionId=selectedId;const top=el('div','version-top'),head=el('div','version-head');head.append(el('span','version-title'),el('span','status'));top.append(head,el('p','version-meta'),el('p','version-text'));const media=el('div','video-wrap');media.append(el('div','video-placeholder'));const controls=el('div','media-controls'),label=el('label','','播放来源'),source=el('select','video-source');source.id='video-source-'+v.version_id;label.htmlFor=source.id;for(const [value,text] of [['raw','原始视频'],['inputs','带输入栏视频']]){const option=el('option','',text);option.value=value;source.append(option)}source.onchange=()=>{const position=activeVideo(card)?.currentTime;card.querySelector('.media-error').textContent='';ensureVideo(card,versions().find(item=>item.version_id===v.version_id)||v,source.value,position);updatePlaybackControls()};controls.append(label,source,el('span','media-error'));card.append(top,media,controls,el('p','job-error'),el('div','version-body'));
  const body=card.querySelector('.version-body'),inputs=el('details','version-inputs');inputs.open=true;inputs.append(el('summary','','本版完整动作时间线与父版输入差异'),el('p','version-plan-explanation input-note'),el('div','action-segments'),el('p','timeline-note input-note muted'),el('div','input-diff'));body.append(el('div','downloads'),inputs,el('div','review-summary'));const form=el('div','review-form'),verdictId='verdict-'+v.version_id,evidenceId='evidence-'+v.version_id;
  const verdictLabel=el('label','','人工总体判断');verdictLabel.htmlFor=verdictId;const verdict=el('select','human-verdict');verdict.id=verdictId;for(const key of ['uncertain','satisfied','unsatisfied']){const option=el('option','',VERDICT[key]);option.value=key;verdict.append(option)}
  const criteria=el('div','review-criteria');for(const [key,labelText] of Object.entries(REVIEW_CRITERIA)){const row=el('div'),label=el('label','',labelText+'（可选）'),select=el('select');select.dataset.criterion=key;select.id=key+'-'+v.version_id;label.htmlFor=select.id;for(const [value,text] of [['','未填写'],...Object.entries(VERDICT)]){const option=el('option','',text);option.value=value;select.append(option)}row.append(label,select);criteria.append(row)}
  const evidenceLabel=el('label','','观察依据与出现时间');evidenceLabel.htmlFor=evidenceId;const evidence=el('textarea','human-evidence');evidence.id=evidenceId;evidence.rows=3;evidence.maxLength=2000;evidence.placeholder='写下看到的事实与时间点，例如：5–8 秒镜头没有向右转。';
  const editLabel=el('label','','希望如何修改（独立指令，不自动执行）'),edit=el('textarea','review-edit');edit.id='review-edit-'+v.version_id;editLabel.htmlFor=edit.id;edit.rows=2;edit.maxLength=2000;edit.placeholder='例如：保留前进，只缩短抬头。此处不是观察打分，也不会自动提交生成。';
  const remember=()=>{const selected={};for(const select of criteria.querySelectorAll('select'))if(select.value)selected[select.dataset.criterion]=select.value;rememberReviewDraft(v.version_id,{verdict:verdict.value,evidence:evidence.value,criteria:selected,edit:edit.value})};verdict.onchange=remember;evidence.oninput=remember;edit.oninput=remember;for(const select of criteria.querySelectorAll('select'))select.onchange=remember;form.append(verdictLabel,verdict,criteria,evidenceLabel,evidence,editLabel,edit,el('p','muted small','草稿保存在当前浏览器；只有“保存人工反馈”成功才写入服务端。修改指令仅供带入编辑框，不属于已提交反馈。'),el('div','review-actions'),el('div','version-actions'),el('p','muted accept-explanation','“我选择此版”仅记录你的选择，不等于自动判好，也不替代人工反馈。'));body.append(form);cards.set(v.version_id,card);return card;
}
function updateCard(v,index){const card=cards.get(v.version_id)||createCard(v),saved=session(),job=jobFor(v),status=jobStatus(v),done=status==='completed',accepted=saved.accepted_version===v.version_id,signature=JSON.stringify([v,job,accepted,busy,hasObserver(),latest()?.version_id,versions().map(item=>[item.version_id,item.retry_of,item.origin,item.root_request])]);
  if(card.dataset.signature===signature)return card;card.dataset.signature=signature;card.className='version'+(accepted?' selected':'');card.querySelector('.version-title').textContent=`版本 ${index+1}${accepted?' · 已选择':''}${v.version_id===latest()?.version_id?' · 最新':''}`;const badge=card.querySelector('.status');badge.textContent=JOB_STATUS[status]||PLAN_STATUS[v.plan?.status]||'计划待生成';badge.className='status '+(['queued','running','completed','failed','cancelled'].includes(status)?status:(['ready','clarify','unsupported'].includes(v.plan?.status)?v.plan.status:''));
  card.querySelector('.version-meta').textContent=[timeLabel(v.created_at),String(v.version_id).slice(0,8),v.parent_version?'基于 '+String(v.parent_version).slice(0,8):'首版',job?.stage?describe(job.stage):''].filter(Boolean).join(' · ');card.querySelector('.version-text').textContent=v.text||'';
  const media=card.querySelector('.video-wrap'),placeholder=media.querySelector('.video-placeholder');placeholder.hidden=done&&!!v.job_id;card.querySelector('.media-controls').hidden=!done;if(done&&v.job_id){ensureVideo(card,v,card.querySelector('.video-source').value)}else{for(const video of media.querySelectorAll('video')){video.pause();video.hidden=true}placeholder.replaceChildren(el('strong','',status?JOB_STATUS[status]||status:'等待确认生成'));if(['running','queued'].includes(status)){if(status==='running'){const progress=el('progress');progress.setAttribute('aria-label','异步生成中，未提供百分比');placeholder.append(progress)}placeholder.append(el('p','','本页会自动更新状态，可以稍后返回。'))}else if(status==='failed'){placeholder.append(el('p','','任务失败；保留错误与原计划，不以示例视频替代。'))}else placeholder.append(el('p','',v.plan?.status==='ready'?'核对动作计划后，可在最新版本提交生成。':'请根据计划说明澄清或修改动作。'))}
  const error=card.querySelector('.job-error');error.textContent=describe(job?.error||v.job_error||v.error||'');error.hidden=!error.textContent;const links=card.querySelector('.downloads');links.replaceChildren();if(done&&v.job_id){for(const [file,label] of [['raw.mp4','↓ 原始视频'],['inputs.mp4','↓ 带输入栏视频']]){const link=el('a','',label);link.href=`/api/jobs/${encodeURIComponent(v.job_id)}/files/${file}?download=1`;link.download=file;links.append(link)}}const exportPlan=el('button','','↓ 计划 JSON');exportPlan.type='button';exportPlan.onclick=()=>downloadJSON('creator-plan-'+v.version_id+'.json',{session_id:saved.session_id,scene_id:saved.scene_id,seed:saved.seed,version_id:v.version_id,base_version_id:v.base_version_id,parent_version:v.parent_version,text:v.text,provider:v.provider,plan:v.plan});links.append(exportPlan);renderInputDetails(card,v);
  const review=v.review;renderReviewSummary(card.querySelector('.review-summary'),v);
  const form=card.querySelector('.review-form'),verdict=form.querySelector('.human-verdict'),evidence=form.querySelector('.human-evidence'),edit=form.querySelector('.review-edit'),draft=getReviewDraft(v.version_id),human=reviewRecords(v).filter(record=>record.source==='human').at(-1);verdict.value=draft?.verdict||human?.verdict||'uncertain';evidence.value=draft?draft.evidence:reviewEvidence(human?.evidence);edit.value=draft?.edit||'';for(const select of form.querySelectorAll('[data-criterion]')){select.value=(draft?.criteria||human?.criteria||{})[select.dataset.criterion]||'';select.disabled=busy||!done}verdict.disabled=busy||!done;evidence.disabled=busy||!done;edit.disabled=busy||!done;
  const actions=card.querySelector('.review-actions');actions.replaceChildren();const save=newAction('保存人工反馈','review',done);save.onclick=()=>{if(!evidence.value.trim()){message('请写下具体观察依据后再保存人工反馈。',true);evidence.focus();return}const criteria={};for(const select of form.querySelectorAll('[data-criterion]'))if(select.value)criteria[select.dataset.criterion]=select.value;rememberReviewDraft(v.version_id,{verdict:verdict.value,evidence:evidence.value,criteria,edit:edit.value});mutate('review',{session_id:selectedId,version_id:v.version_id,verdict:verdict.value,evidence:evidence.value.trim(),criteria},'人工反馈已保存；修改指令尚未编排，也未提交视频任务。')};actions.append(save);const useEdit=newAction('基于此版修改','use-review-edit',done&&v.plan?.status==='ready');useEdit.title='固定此版为编辑基线并填入自然语言输入框；不调用模型、不编排、不提交生成。下方对比选择独立于编辑基线。';useEdit.onclick=()=>useReviewEdit(v.version_id,edit.value);actions.append(useEdit);
  const inspect=newAction(hasObserver()?'请求视觉观察':'视觉观察未配置','inspect',done&&hasObserver()&&review?.source!=='human');inspect.title=hasObserver()?'按需请求一次视觉观察；已有人工审核时，不覆盖人工判断。':'当前仅人工审核；不会伪造自动观察结果。';inspect.onclick=()=>mutate('inspect',{session_id:selectedId,version_id:v.version_id},'视觉观察已返回，请检查来源与依据。');actions.append(inspect);
  const choices=card.querySelector('.version-actions');choices.replaceChildren();if(status==='failed'){const canRetry=!!config.generation_enabled&&v.plan?.status==='ready'&&Number(v.retry_count||0)<2&&!versions().some(item=>item.retry_of===v.version_id),retry=newAction('重试同一计划','retry',canRetry);retry.title='保持同一计划、首图与种子，直接提交新的生成任务，不再调用编排模型；保留失败证据，最多重试两次。';retry.onclick=()=>mutate('retry',{session_id:selectedId,version_id:v.version_id},'已用同一计划提交新的生成任务。旧版本和失败任务仍会保留。',true);choices.append(retry)}const accept=newAction(accepted?'已由你选择':'我选择此版','accept',done&&!accepted);accept.classList.add('accept');accept.onclick=()=>mutate('accept',{session_id:selectedId,version_id:v.version_id},'已记录你的版本选择，不代表自动质量判定；其他版本与原始证据仍会保留。');choices.append(accept);
  const count=Number(v.automatic_revisions??saved.auto_revisions??saved.auto_revision_count??0),max=Number(config.max_auto_revisions??1),alreadyRevised=versions().some(item=>item.origin==='visual_feedback'&&item.root_request===v.root_request),canRevise=config.visual_revision_enabled===true&&v.version_id===latest()?.version_id&&done&&hasObserver()&&review?.source==='model_assessment'&&review?.decision==='revise'&&review?.verdict==='unsatisfied'&&count<max&&!alreadyRevised;const revise=newAction(config.visual_revision_enabled===true?'依据观察修订一次':'未校准 · 请人工审核','revise',canRevise);revise.title=config.visual_revision_enabled===true?'仅最新版本可依据模型观察修订。仅编排新计划，不自动提交生成；每个请求最多一次。人工修改请在上方描述中继续。':'视觉反馈修订默认关闭，只有操作方显式启用后可用；当前请人工审核并明确修改描述。';revise.onclick=()=>mutate('revise',{session_id:selectedId,version_id:v.version_id},'已根据反馈编排修订计划。请核对后再确认生成。',true);choices.append(revise);return card;
}
function renderVersions(){const all=versions(),chosen=comparison(),visible=[chosen.left,chosen.right].map(id=>all.find(v=>v.version_id===id)).filter(Boolean),box=$('versions');$('empty-versions').hidden=all.length>0;renderComparisonControls();const ids=new Set(visible.map(v=>v.version_id));for(const [id,card] of cards){const hide=card.dataset.sessionId!==selectedId||!ids.has(id);if(hide&&!card.hidden)for(const video of card.querySelectorAll('video'))video.pause();card.hidden=hide}for(const [index,v] of visible.entries()){const card=updateCard(v,all.indexOf(v));card.hidden=false;card.dataset.side=chosen.left===v.version_id?'left':'right';card.style.order=String(index);if(card.parentNode!==box)box.append(card)}
  const accepted=session()?.accepted_version;$('accepted-note').hidden=!accepted;if(accepted){const index=all.findIndex(v=>v.version_id===accepted);$('accepted-note').textContent=`你的当前选择：${index>=0?'版本 '+(index+1):String(accepted).slice(0,8)}。这是用户选择，不是自动质量判定；全部历史版本仍可回看。`}updatePlaybackControls();
}
function render(){renderSessionOptions();renderPlan();renderVersions();updateControls()}
async function refresh(){if(refreshing||busy)return;refreshing=true;try{const results=await Promise.allSettled([api('/api/sessions'),api('/api/jobs')]);let error='';if(results[0].status==='fulfilled'){sessions=results[0].value.sessions||[];if(selectedId&&!sessions.some(s=>s.session_id===selectedId)){selectedId='';storageSet('interactworld.creator.session','')}render()}else error='会话读取失败：'+results[0].reason.message;
  if(results[1].status==='fulfilled'){const jobs=results[1].value.jobs||[];jobsById=new Map(jobs.map(j=>[j.job_id,j]));const count=status=>jobs.filter(j=>j.status===status).length;$('queue-summary').textContent=`${count('running')} 个处理中 · ${count('queued')} 个等待 · ${count('completed')} 个已完成`;renderVersions();updateControls()}else error+=(error?'\n':'')+'任务状态读取失败：'+results[1].reason.message;if(error)message(error+'\n页面会继续读取状态，不会自动重放生成请求。',true);
}finally{refreshing=false}}
async function mutate(operation,body,success,idempotent=false,onSuccess){if(busy)return;busy=true;updateControls();let request;try{if(idempotent){request=requestId(operation,body);body={...body,request_id:request.id}}const response=await api('/api/'+operation,body);upsertSession(response);if(['plan','revise','retry'].includes(operation)&&!(response.planned_version_id||response.session?.planned_version_id))rememberPlannedVersion(latest()?.version_id);if(request)requestIds.delete(request.key);if(onSuccess)onSuccess();alignSessionInputs();render();message(success)}catch(error){message('操作失败：'+error.message,true)}finally{busy=false;render();await refresh()}}
function selectSession(id){selectedId=id;storageSet('interactworld.creator.session',id);planSignature='';$('instruction').value='';if(id)alignSessionInputs();else{const s=scene();$('seed').value=s?.seed??42;renderScene()}render()}
$('scene').onchange=()=>{if(!session())$('seed').value=scene()?.seed??42;renderScene();updateControls()};$('seed').oninput=updateControls;$('instruction').oninput=updateControls;$('session').onchange=()=>selectSession($('session').value);$('new-session').onclick=()=>{selectSession('');$('instruction').focus()};
$('planner-mode').onchange=updateControls;
$('edit-base-version').onchange=()=>{if(!busy&&setEditBase($('edit-base-version').value)){renderEditBase();updateControls()}};
$('plan').onclick=()=>{const error=validateEditor();if(error){message(error,true);return}mutate('plan',planPayload(),'动作计划已保存。请核对本次实际基线、按键与时长，再确认生成。',true)};
$('generate').onclick=()=>{const v=plannedVersion();if(!canGenerate(v)||busy)return;mutate('generate',{session_id:selectedId,version_id:v.version_id},'生成任务已提交。你可以在下方查看状态，刷新页面也能恢复。',true)};
$('compare-left').onchange=()=>chooseComparison('left',$('compare-left').value);$('compare-right').onchange=()=>chooseComparison('right',$('compare-right').value);$('compare-latest').onclick=()=>{comparison().follow=true;saveComparison();renderVersions()};$('download-session').onclick=()=>{const saved=session();if(saved)downloadJSON('creator-session-'+saved.session_id+'.json',saved)};
$('play-together').onclick=()=>playTogether(false);$('restart-together').onclick=()=>playTogether(true);$('pause-together').onclick=()=>{for(const video of comparisonVideos())video?.pause();updatePlaybackControls()};
document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh()});
(async()=>{try{config=await api('/api/config');initializePlannerMode();for(const s of config.scenes||[]){const option=el('option','',s.name||s.scene_id);option.value=s.scene_id;$('scene').append(option)}$('planner-kind').textContent=plannerLabel(config.planner_kind)+(config.provider?' · '+describe(config.provider):'');$('observer-kind').textContent=observerLabel();$('seed').value=scene()?.seed??42;selectedId=storageGet('interactworld.creator.session');renderScene();await refresh();alignSessionInputs();render();if(!config.generation_enabled)message('当前是编排预览模式。可以保存动作计划，生成需要操作员启用授权与预算门禁。');if(!(config.scenes||[]).length)message('尚未配置可用场景，请由操作员配置固定首图与提示词。',true);setInterval(()=>{if(!document.hidden)refresh()},3000)}catch(error){message('页面初始化失败：'+error.message+'。请检查服务后刷新重试。',true)}})();
'''
